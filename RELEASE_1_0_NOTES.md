# 又寄了 1.0 本地可用版

本版基于已通过本地验收的 Release Candidate，集中完成两个上线前问题：个人资料与手动记账可靠性。

## 个人资料

- 新增 `public.profiles`：昵称、头像路径、默认支付方式、货币。
- “我的”页面支持编辑昵称。
- 支持上传/更换/移除头像；头像存入 Supabase 私有 `avatars` Storage bucket。
- 头像只允许 JPG/PNG/WebP，最大 3MB；Storage RLS 只允许用户访问自己目录。
- 默认支付方式支持：未指定、微信、支付宝、银行卡、现金、其他。
- 快速记账和手动记账都会使用用户默认支付方式；若设为“未指定”则不自动猜。
- 邮箱继续作为 Supabase 登录身份，只读显示；人民币 CNY 作为当前固定货币。

## 手动记账修复

原 RC 的手动记账存在几个可用性问题：

- “标题 / 备注”在界面上像可选项，代码却标记为 `required`，导致不填写时浏览器直接阻止提交。
- 保存失败只通过全局 toast 呈现，在模态框里不够明显。
- 金额使用原生 number 控件，在桌面浏览器显示上下箭头，输入体验不稳定。

1.0 已调整：

- 只有金额和日期属于真正必需信息。
- 标题/备注可留空；自动使用细分类或一级分类作为标题。
- 金额改为 decimal 文本输入，自动过滤非法字符并格式化为两位小数。
- 无效金额直接在表单内提示，保存按钮不可用。
- 云端保存失败会在模态框内显示错误，不再“点了没反应”。
- 编辑已有账单继续保留分类学习、删除和撤销功能。

## 数据库升级

已有用户只需在 Supabase SQL Editor 执行一次：

`supabase/upgrade_to_1_0.sql`

该脚本包含此前 Release Candidate 的数据结构升级，并新增 profiles / avatars。它不会删除现有账单。
