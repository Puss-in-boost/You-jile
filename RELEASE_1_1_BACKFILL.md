# 又寄了 1.1 — 历史补账工作流

这一版基于已经验收可用的 1.0.1 数据/登录/资料/手动记账能力，新增「补旧账」模式。

## 解决的问题

过去补历史账时，每一笔都需要：打开记账弹窗 → 选日期 → 保存 → 再打开下一笔。

现在在「账单」页点击「补旧账」后：

- 选择一次日期，日期会锁定。
- 连续输入多笔，按 Enter 保存后输入框保持焦点。
- 点击「前一天 / 后一天」直接切换，不退出工作流。
- 实时显示当天已记录的账单、支出/收入小计。
- 支持一次粘贴多行账单，预览解析结果后批量保存。
- 支持 `Alt + ← / →` 切换日期，`Esc` 退出。
- 记住上一次补账日期，下次打开可继续接着补。

## 多行批量示例

```text
35 午饭
20 瑞幸 微信
16 地铁 支付宝
68 晚饭
```

每行都会沿用当前锁定日期；分类、细分类、支付方式和个人默认支付方式继续使用现有解析规则。

## 数据库

本版不新增数据库字段，不需要执行新的 Supabase SQL。
