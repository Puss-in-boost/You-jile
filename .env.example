# 推荐：Supabase 云端账本模式。只需要下面两个公开浏览器配置。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
DISABLE_DEMO=true

# 可选：仅本地 PostgreSQL / 旧 API 兼容模式需要。
# Supabase 云端日常使用不需要 DATABASE_URL。
DATABASE_URL=
