# 又寄了 1.2.1 — 数字名称与编辑修复

本次修复针对“168 88会员 / 淘宝会员”这类真实补账场景。

## 修复内容

- 快速记账不再在解析金额前删除空格，避免 `168 88会员` 被拼成 `16888会员`。
- 数字可以属于商品/服务名称：`88会员`、`88VIP`、`Microsoft 365`、`Office 365`、`12306`、`1688` 等不会误当金额。
- 约定更明确：`168 88会员`、`淘宝88VIP 168` 都能正确识别 ¥168。
- 新增 `订阅服务 / 平台会员` 细分类：淘宝88VIP、京东PLUS、美团会员、饿了么会员等优先于“淘宝/京东=电商购物”的泛化规则。
- 修复编辑已有账单时把 `createdAt/userId/updatedAt` 等数据库元数据一起送入草稿校验的问题；现在编辑只校验真正可编辑字段。
- 编辑失败时会指出具体字段，而不是只显示笼统的“账单内容无效”。
- 附带 `supabase/upgrade_to_1_2_1_membership.sql`，可自动纠正已经录入的淘宝/88VIP/京东PLUS等会员账单。

## 验证

核心 parser / 分类 / 统计测试共 27 项通过，包括：

- `168 88会员 -> ¥168 / 订阅服务 / 平台会员`
- `淘宝88VIP 168 -> ¥168 / 订阅服务 / 平台会员`
- `168 淘宝会员 -> ¥168 / 订阅服务 / 平台会员`
- `淘宝买衣服 -> 购物 / 电商购物`
- `午饭35奶茶20` 仍会拒绝为多金额歧义输入
