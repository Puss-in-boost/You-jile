# Release Candidate 收口说明

这不是新的试验分支，而是把此前 V2–V5 的补丁合并成一个日常使用基线。

本轮一次性收口：

1. 云端正式链路统一为 Supabase SDK + RLS；Supabase 模式不再要求 DATABASE_URL。
2. 旧数据库使用单一 `upgrade_existing_to_release.sql` 升级，不再按 V4/V5 多次迁移。
3. 支付方式默认“未指定”，快速记账只在用户明确写出支付方式时识别。
4. 一级分类 + 细分类正式落地；瑞幸/星巴克等归 `餐饮 / 咖啡饮品`。
5. 账单页增加类型、分类、账户完整筛选。
6. 洞察增加上月对比与每日支出趋势。
7. 快速记账离线时禁止保存，避免误认为云端已写入。
8. 语音/OCR 明确标记“即将上线”，不再使用看起来已可用的假入口。
9. 常见 Supabase/Auth/RLS/网络错误中文化。
10. 补齐 192/512 PWA 图标与 Apple Touch Icon，解决此前图标 404。
11. `/api/health` 与服务器构建不再因为未配置 DATABASE_URL 在模块加载阶段直接崩溃。
12. 增加 `npm run doctor` 与 `npm run check`。

当前“可日常使用”定义：文字/手动记账、云同步、编辑删除撤销、分类学习、筛选、洞察、账号安全、CSV 备份和 PWA 均属于正式功能；语音/OCR/离线写入属于后续新增能力。
